apiVersion: v1
kind: ConfigMap
metadata:
  name: asyncapijson-config
  #namespace: names Si no se indica namespace lo introducira en el mismo namespace de la aplicación
data:
  DATABASE_NAME: {{ .Values.configMapRef.data.DATABASE_NAME }}
  DATABASE_PORT: "3306"
  DATABASE_HOST: {{ .Values.configMapRef.data.HOST | quote }}